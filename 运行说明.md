# 船行问题解题助手 - 本地运行指南

## 环境要求

- **Node.js** 18.0 或更高版本
- **npm** 或 **pnpm** 包管理器

## 安装步骤

### 1. 安装 Node.js

如果还没有安装 Node.js，请访问官网下载安装：
- 官网：https://nodejs.org/
- 建议下载 LTS（长期支持）版本

安装完成后，在终端中验证：
```bash
node -v
npm -v
```

### 2. 进入项目目录

```bash
cd /path/to/ship-problem-solver
```

将 `/path/to/ship-problem-solver` 替换为实际的项目路径

### 3. 安装依赖

```bash
npm install
```

或者如果你使用 pnpm：
```bash
pnpm install
```

### 4. 启动开发服务器

```bash
npm run dev
```

或：
```bash
pnpm dev
```

### 5. 访问网页

启动成功后，终端会显示类似以下信息：
```
VITE v6.4.3  ready in 227 ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

打开浏览器，访问：**http://localhost:5173/**

## 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览生产构建 |

## 项目结构

```
ship-problem-solver/
├── src/
│   ├── pages/
│   │   └── Home.tsx      # 主页面组件
│   ├── App.tsx           # 应用入口
│   └── index.css         # 全局样式
├── index.html            # HTML模板
├── package.json          # 项目配置
└── vite.config.ts        # Vite配置
```

## 常见问题

### 端口被占用
如果 5173 端口被占用，Vite 会自动尝试其他端口，查看终端输出获取实际端口号。

### 依赖安装失败
尝试清除缓存后重新安装：
```bash
rm -rf node_modules package-lock.json
npm install
```

### 浏览器无法访问
- 检查防火墙设置
- 尝试使用 `--host` 参数：`npm run dev -- --host`
